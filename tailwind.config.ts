// Placeholder Tailwind configuration (to be configured later)
import type { Config } from 'tailwindcss'

const config: Config = {
	content: [],
	theme: { extend: {} },
	plugins: [],
}

export default config
